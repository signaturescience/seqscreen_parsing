pylint:
  stage: test
  image: python:3.7
  before_script:
  script:
    - mkdir ./pylint
    - set -e
    - set -o pipefail
    - shopt -s globstar
    - pylint ./**/*.py -ry --output-format=text | tee ./pylint/pylint.txt || pylint-exit ${PIPESTATUS[0]}